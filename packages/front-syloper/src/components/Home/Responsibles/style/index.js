import styled from 'styled-components';

const ResponsiblesMain = styled.article`
  width: 100%;
  height: calc(100% - 100px);
  overflow-y: scroll;
  overflow-x: hidden;
`;

export { ResponsiblesMain };
